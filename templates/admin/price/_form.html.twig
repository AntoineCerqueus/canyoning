{% block content %}

{{ form_start(form) }}

{{ form_widget(form) }} 

    <button class="btn btn-success text-body bottom">{{ button_label|default('Créer') }}</button>
    
{{ form_end(form) }}
{% endblock %}
